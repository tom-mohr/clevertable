# The only reason why this setup.py exists, is to allow for
# editable installs with pip during development of this package.
# Editable mode currently requires a setup.py based build.

from setuptools import setup

setup()
